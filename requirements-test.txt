# Test + schema tooling for the API test suite.
# Install with:  pip install -r requirements-test.txt
pytest==9.1.1
pytest-django==4.12.0
pytest-cov==7.1.0
factory-boy==3.3.3
Faker==40.28.1

# Linting/formatting — matches the pinned CI check (informational for now)
ruff==0.15.17

# OpenAPI schema snapshot validation (manage.py generate_swagger -> openapi.yaml)
openapi-spec-validator==0.9.0

# Dependency vulnerability scanning (run: pip-audit -r requirements.txt)
pip-audit==2.10.1
